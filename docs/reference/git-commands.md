# Git 基本コマンド

## 最初の設定

```bash
git config --global user.email "メールアドレス"
git config --global user.name "isseizenei0924"
```

## ファイルをGitHubに保存する流れ

```bash
git add .
git commit -m "変更内容のメモ"
git push
```

## 状態確認

```bash
git status
```
